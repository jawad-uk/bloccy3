<h1>Edit User</h1>

<%= form_for @user do | f | %>
	<p>
		<%= f.label :name %><br/>
		<%= f.text_field :name %>
	</p>

	<p>
		<%= f.label :email %>
		<%= f.text_field :email %>
	</p>

	<p>
		<%= f.submit "Update user" %>
	</p>

<% end %>